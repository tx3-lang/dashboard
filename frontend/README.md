# tx3 Dashboard — frontend

TanStack Start (Nitro SSR) app that reads `tracker.db` via Kysely + better-sqlite3. See the root [`README.md`](../README.md) for the architecture and [`docs/running.md`](../docs/running.md) for prerequisites and first-run steps.

Scripts: `pnpm dev`, `pnpm build`, `pnpm test`, `pnpm typecheck`, `pnpm check`.
